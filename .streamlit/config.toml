[server]
headless = true
runOnSave = true
allowRunOnSave = true
fileWatcherType = "auto"
# Serves ./static at /app/static — used for "Scan in neuem Tab öffnen"
# (second-monitor workflow).
enableStaticServing = true
